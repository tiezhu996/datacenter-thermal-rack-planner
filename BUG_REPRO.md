# Bug 复现说明

## Bug 是什么
热区邻接跨层不一致：评分把自身热量当邻居热计算；自邻接校验缺失；邻接键大小写不归一；损坏邻接 JSON 返回 null。

## 如何触发
1. 配置邻接关系后评估布局，评分不反映邻居热量；
2. 创建把自己配成邻居的热区 → 不报 422；
3. 用小写邻接键保存 → 不生效且不回写大写；
4. 直接改坏 adjacency_json → GET 返回 null。

## 错误信息
无显式报错；错误表现为评分失真、自邻接可保存、邻接键不归一、响应邻接字段为 null。
