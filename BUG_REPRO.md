# Bug 复现说明

## Bug 是什么
机柜/热区创建与更新用手工事务 + defer 覆盖命名返回值，写错误被吞掉；机柜更新跳过存在性校验，缺失行数检查；热区查询缺失返回空对象。重复/版本冲突/不存在均未正确报错。

## 如何触发
1. 创建两个同编码机柜 → 第二次返回 404（应 409）；
2. 用旧版本号 PUT /api/v1/racks/:id → 200 覆盖成功并多写审计（应 409）；
3. 创建重复热区编码 → 返回 201 且 id=0（应 409）；
4. GET /api/v1/zones/9999 → 200 空数据（应 404）。

## 错误信息
无显式报错；错误表现为 404/201-id0/200 覆盖等错误状态码与多余审计记录。
