# Bug 复现说明

## Bug 是什么
布局评估不再复制输入切片直接原地排序，调用方传入的热区/机柜/负载顺序被改写；评估结果复用共享缓冲区，连续两次评估第一次的结果被第二次覆盖。

## 如何触发
1. 以特定顺序传入 zones/racks/loads 调用 Engine.Evaluate；
2. 调用后检查输入切片顺序被排序改写；
3. 连续评估两次，第一次返回的 Assignments 内容被第二次覆盖。

## 错误信息
无报错；错误表现为输入切片顺序被改、首次评估结果被后续评估覆盖。
